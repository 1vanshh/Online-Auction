server:
  port: ${AUCTION_SERVICE_PORT:8082}

spring:
  config:
    import: optional:file:.env[.properties],optional:file:../../.env[.properties]

  application:
    name: auction-service

  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:postgres}:${POSTGRES_PORT:5432}/${POSTGRES_DB:online_auction}
    username: ${POSTGRES_USER:postgres}
    password: ${POSTGRES_PASSWORD:password}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: auction
        format_sql: true
    open-in-view: false

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 6MB

  flyway:
    enabled: true
    default-schema: auction
    schemas: auction
    locations: classpath:db/migration

logging:
  level:
    root: INFO
    org.springframework.web: INFO
    org.hibernate.SQL: INFO
    org.flywaydb: INFO
    com.auction.auctionservice: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: never

jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION_MS:3600000}
  refresh-expiration: ${JWT_REFRESH_EXPIRATION_MS:2592000000}
auction:
  upload-dir: ${AUCTION_UPLOAD_DIR:uploads/lots}
  max-image-size-bytes: ${AUCTION_MAX_IMAGE_SIZE_BYTES:5242880}
